// Service Worker — Development mode
// In development, this worker does minimal caching to support fast iteration.
// The production worker (service-worker.published.js) handles full precaching.

self.addEventListener('fetch', () => { });
